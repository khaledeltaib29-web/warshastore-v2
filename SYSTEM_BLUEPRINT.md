# 🛠️ دليل النظام والهيكلية الفنية والتنفيذية لتطبيق WarshaStore
## (WarshaStore System Blueprint & AI Maintenance Guide)

---

## 1. 📁 هيكل المشروع وتوزيع الملفات والمجلدات (File Structure)

```
WarshaStore/
├── index.html                       # الملف الرئيسي لتشغيل تطبيق React في المتصفح
├── src/
│   ├── main.tsx                    # نقطة الانطلاق الرئيسية للتطبيق (React Root)
│   ├── App.tsx                     # إدارة الحالة المركزية (Central State Management & Routes)
│   ├── types.ts                    # جميع الواجهات والأنواع (TypeScript Interfaces & Types)
│   ├── data.ts                     # البيانات التأسيسية الأولية والبيانات المخزنة افتراضياً
│   ├── index.css                   # إعدادات وتنسيقات Tailwind CSS الشاملة
│   ├── components/                 # مكونات الواجهة البرمجية الموديلية
│   │   ├── Header.tsx              # شريط الهيدر العلوي، أزرار التزامن، زر المتصلين واللغة
│   │   ├── Navigation.tsx          # شريط التنقل بين الصفحات والتبويبات المخصصة للصلاحيات
│   │   ├── ActiveUsersModal.tsx    # نافذة شاشة تتبع المتصلين الآن (Real-time Active Users Monitor)
│   │   ├── NotificationsModal.tsx  # مركز التنبيهات الإدارية، طلبات الاعتماد الأرشيف والـ Pending Approvals
│   │   ├── LoginModal.tsx          # نافذة تسجيل الدخول وتبديل الحسابات
│   │   ├── OrderFormModal.tsx      # نموذج إضافة وتعديل الطلبات (الأوردرات)
│   │   ├── ProductFormModal.tsx    # نموذج إضافة وتعديل المنتجات والمخزون
│   │   ├── ExpenseFormModal.tsx    # نموذج إضافة وتعديل المصروفات
│   │   ├── ExpenseCategoriesModal.tsx # إدارة تصنيفات المصروفات المخصصة
│   │   ├── ManufacturerFormModal.tsx # نموذج إضافة وتعديل الورش المصنعة
│   │   ├── ManufacturerStatementModal.tsx # كشف حساب ورشة تصنيع وحركات السداد وتوليد الفواتير
│   │   ├── UserManagementView.tsx  # شاشة إدارة المستخدمين والسجلات الرقابية (Audit Trail)
│   │   ├── DashboardView.tsx       # لوحة التحليلات والمؤشرات الإحصائية العامة
│   │   ├── OrdersView.tsx          # شاشة جدول الطلبات مع الفلترة والتصدير واستخراج الفواتير
│   │   ├── InventoryView.tsx       # شاشة إدارة مخزون المنتجات والتنبيه بنقص المخزون
│   │   ├── ExpensesView.tsx        # شاشة سجل المصروفات والتحليلات المالية
│   │   ├── ManufacturersView.tsx   # شاشة دليل الورش والمديونيات
│   │   ├── RemindersView.tsx       # شاشة التذكيرات والمهام الإدارية
│   │   ├── AnalyticsView.tsx       # شاشة التحليلات المتقدمة والتقارير
│   │   ├── SystemSettingsView.tsx  # إعدادات النظام ومزامنة Google Sheets وواتساب
│   │   └── PrintInvoiceModal.tsx   # نافذة طباعة وتصدير الفاتورة الرسمية (PDF/Print)
├── metadata.json                   # وصف التطبيق والصلاحيات
├── package.json                    # مكتبات وتطبيقات المشروع
└── SYSTEM_BLUEPRINT.md             # ملف التوثيق الشامل الحاضر
```

---

## 2. 👥 تفاصيل الأدوار والصلاحيات (Roles & Permissions Architecture)

تتم إدارة الصلاحيات بنظام صارم لمنع التداخل أو تسريب البيانات بين الحسابات:

| الدور (UserRole) | الاسم في النظام | الصلاحيات البرمجية والحدود المسموحة |
| :--- | :--- | :--- |
| `super_admin` | **المدير العام** | صلاحيات كاملة ومطلقة للنظام: إضافة/تعديل/حذف مباشر بدون معالجة انتظار، اعتماد أو رفض طلبات التعديل والحذف، إدارة المستخدمين، الاطلاع على كشوف الحسابات المتقدمة، شاشة المتصلين الآن، وعرض جميع التنبيهات والأرشيف. |
| `deputy_admin` | **النائب العام** | صلاحيات واسعة تشبه المدير العام مع تحويل أفعال الحذف والتعديل الحساسة إلى طلبات انتظار (`Pending Approvals`) ترفع للمدير العام، الوصول لشاشة المتصلين الآن والتقارير العامة. |
| `accountant` | **مسؤول العمليات / المحاسب** | إدارة العمليات اليومية (إضافة أوردرات، إضافة مصروفات، متابعة المخزون)، لا يمكنه الحذف المباشر بل يرسل طلب اعتماد (`Pending Approval`)، له صلاحية رؤية شاشة المتصلين الآن. |
| `manufacturer` | **ورشة مصنعة** | **عزل تام وسري (Data Isolation)**: لا يرى سوى الأوردرات المسندة لورشته والمنتجات المخصصة له فقط، لا يرى أرباح النظام، لا يرى أسعار البيع النهائية، ولا يرى المصروفات أو الشاشات المالية الإدارية. |

---

## 3. 🛡️ نظام الأمان والموافقات والأرشفة (Audit Trail & Pending Approvals Engine)

1. **طلبات التعديل والحذف المعلقة (`pendingApprovals`)**:
   - عندما يحاول مستخدم (غير `super_admin`) تعديل أو حذف أوردر، منتج، مصروف، أو ورشة، لا يتم التعديل في قاعدة البيانات فوراً.
   - يتم إنشاء طلب معلق يحتوي على تفاصيل الطلب (`targetType`, `actionType`, `payload`, `requesterName`, `reason`).
   - يتلقى المدير العام تنبيهاً فورياً بمباشرة طلب الاعتماد في نافذة التنبيهات.
   - عند **الاعتماد (Approve)**: يتم تنفيذ التعديل أو الحذف فوراً تلقائياً وتحديث كشوف الحسابات التابعة.
   - عند **الرفض (Reject)**: يتم إلغاء الطلب وأرشفته مع إشعار صاحب الطلب بالرفض.

2. **سجل السجلات الرقابية (`auditLogs`)**:
   - تسجيل رقمي زمني حاد لكافة الحركات الهامة للنظام: تسجيل الدخول، إنشاء الطلبات، تغيير الحالات، السداد، وطلبات الحذف والتعديل.
   - يتضمن السجل: معرف المستخدم، اسم الحساب، نوع الحركة، النوع المستهدف، والتفاصيل الدقيقة والوقت.

---

## 4. 📡 ميزة تتبع المتصلين الآن (Active Users Monitor)

- **شاشة تتبع فورية وحصرية**: تظهر في الهيدر العلوي لـ (`super_admin`, `deputy_admin`, `accountant`).
- **تفاصيل التتبع اللحظي**:
  - **اسم المستخدم والاسم الكامل**: (`name`, `@username`).
  - **عنوان الـ IP والموقع الجغرافي**: (مثال: `197.38.14.82 (مصر - متصل)`).
  - **وقت الدخول الرسمي**: وقت وتاريخ بداية الجلسة.
  - **وقت آخر ظهور ونشاط**: تحديث لحظي ومباشر بآخر تفاعل داخل التطبيق.
  - **بيانات الجهاز والمتصفح**: (نوع الجهاز: جوال / كمبيوتر ونوع المتصفح).
  - **تحديث الخروج**: يزال الحساب فوراً من شاشة المتصلين عند الضغط على تسجيل الخروج (`Logout`).

---

## 5. 📊 آلية الربط بقاعدة بيانات Google Sheets وجداول النظام

1. **الهيكلية وتنسيق المزامنة (`handleSyncToGoogleSheets`)**:
   - يتصل التطبيق بـ **Google Sheets API** المباشر لتصدير المبيعات، الطلبات، الورش، والمصروفات.
   - **المزامنة الصامتة اللحظية**: عند تفعيل خيار التحديث التلقائي في الإعدادات، يتم تحديث البيانات فور كل إضافة أو تعديل دون إزعاج المستخدم.
2. **المخزن المحلي والضمانات (`localStorage Persistence`)**:
   - مفاتيح التخزين الرئيسية بالنظام:
     - `warsha_orders`: جدول الطلبات والأوردرات.
     - `warsha_products`: دليل المنتجات والمخزون.
     - `warsha_expenses`: جدول المصروفات.
     - `warsha_manufacturers`: دليل الورش والمديونيات.
     - `warsha_users`: حسابات المستخدمين والصلاحيات.
     - `warsha_pending_approvals`: طلبات الموافقات المعلقة.
     - `warsha_active_sessions`: الجلسات النشطة المتصلة بالنظام.
     - `warsha_audit_logs`: سجل النشاطات الحساسة.

---

## 6. 🤖 دليل المساعدة للذكاء الاصطناعي المستقبلي (AI Developer Maintenance Rules)

> ⚠️ **تنبيه حاسم لأي نموذج ذكاء اصطناعي يقوم بالتعديل على الكود مستقبلاً:**

1. **حظر إعادة تهيئة قاعدة البيانات (DO NOT Reset Database)**:
   - يمنع منعاً باتاً مسح الـ `localStorage` أو استبدال القيم المخزنة بقيم فارغة.
   - عند إضافة حقول جديدة لأي كائن (مثل Order أو Product)، استخدم الاختيار الآمن (`optional chaining ?.`) أو توفير قيم افتراضية دون تدمير السجلات القائمة.

2. **الحفاظ على سلامة حاسبة المستحقات والورش**:
   - وظيفة `recalculateManufacturersFromDeliveredOrders` تُحسب تلقائياً وبدقة إجمالي مستحقات الورش بناءً على الأوردرات المكتملة ذات حالة **"تم التسليم"** مطروحاً منها الدفعات المسددة. لا تقم بتعديل معادلات هذه الدالة دون مراجعة كشف الحساب.

3. **التحقق من الصلاحيات والـ Optional Chaining**:
   - تأكد دائماً من استخدام `currentUser?.role` في أجهزة الشرط لمنع أخطاء `TypeError: Cannot read properties of undefined` في حال كانت الجلسة مسجلة خروج.

4. **تحديث شاشة المتصلين والمزامنة اللحظية**:
   - حافظ على تشغيل ميزة `ActiveUsersModal` والمزامنة الفورية عند تعديل الهيدر أو إضافة صفحات جديدة.
